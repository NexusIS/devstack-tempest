# Control node

HOST_IP=192.168.56.11
HOST_NAME=$(hostname)

FLAT_INTERFACE=eth1
FLOATING_RANGE=192.168.56.128/25
FIXED_RANGE=10.4.128.0/20
FIXED_NETWORK_SIZE=4096
PUBLIC_NETWORK_GATEWAY=192.168.56.1

Q_USE_SECGROUP=True
Q_PLUGIN=ml2                  # Should we use openvswitch plugin instead???
ENABLE_TENANT_TUNNELS=True
Q_ML2_TENANT_NETWORK_TYPE=vxlan
Q_ALLOW_OVERLAPPING_IP=True
    
MULTI_HOST=1

LOGFILE=/opt/stack/logs/stack.sh.log
SCREEN_LOGDIR=/opt/stack/logs/screen
VERBOSE=True
LOG_COLOR=True

CINDER_BRANCH=stable/havana
GLANCE_BRANCH=stable/havana
HORIZON_BRANCH=stable/havana
KEYSTONE_BRANCH=stable/havana
KEYSTONECLIENT_BRANCH=stable/havana
NOVA_BRANCH=stable/havana
NOVACLIENT_BRANCH=stable/havana
NEUTRON_BRANCH=stable/havana
SWIFT_BRANCH=stable/havana

DATABASE_PASSWORD=password
RABBIT_PASSWORD=password
SERVICE_PASSWORD=password
ADMIN_PASSWORD=password
SERVICE_TOKEN=xyzpdqlazydog

API_RATE_LIMIT=False

# Controller Node services
ENABLED_SERVICES=rabbit,mysql,key,tempest,horizon
ENABLED_SERVICES+=,n-api,n-crt,n-obj,n-cpu,n-cond,n-sch
ENABLED_SERVICES+=,g-api,g-reg
IMAGE_URLS+=",https://launchpad.net/cirros/trunk/0.3.0/+download/cirros-0.3.0-x86_64-disk.img"

# Neutron agent
ENABLED_SERVICES+=,neutron,q-agt

# Network Node services
ENABLED_SERVICES+=,q-svc,q-dhcp,q-l3,q-meta

# Don't use Nova Network
ENABLED_SERVICES+=,-n-net

# Run a scheduler that will balance VMs across hosts
SCHEDULER=nova.scheduler.filter_scheduler.FilterScheduler
